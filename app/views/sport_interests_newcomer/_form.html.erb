<%= form_with(model: sport_interests_newcomer, local: true) do |form| %>
  <% if sport_interests_newcomer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sport_interests_newcomer.errors.count, "error") %> prohibited this sport_interests_newcomer from being saved:</h2>

      <ul>
      <% sport_interests_newcomer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
